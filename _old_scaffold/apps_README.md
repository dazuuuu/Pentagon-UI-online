# Apps layout

- Public site files belong in public/ and are served from the web root.
- Backend/admin/client/api entry points are routed via apps/router.php.
- Database defaults now target MySQL via apps/backend/config.php.
- Site branding and hero content can be edited from /admin/settings.php.
- SMTP is configured from the same admin screen and tested from there.
